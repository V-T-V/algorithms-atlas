// =============================================================================
// 辅助：向下取整的 log2
// =============================================================================

export function log2floor(n: number): number {
  if (n <= 0) return -1;
  let k = 0;
  while (1 << (k + 1) <= n) k++;
  return k;
}
